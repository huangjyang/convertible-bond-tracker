# 可转债跟踪 · 小程序版本方案

> 2026-09-18 · 前提：**只自己用，不对外发布、不告诉别人**
> 结论：**可行，但"自用"有一个技术坑跟合规无关——小程序取不到你 Mac 上 `127.0.0.1:8734` 的数据。**
>
> **进度：方案 0（手机端适配 + 添加到主屏幕）已于 2026-09-18 落地**，实现细节与验证方式见
> `.workbuddy/memory/2026-09-18.md`。下面第 4、5 节（小程序前端改写清单）仍是"真要写小程序时"的方案，
> 自用分支不需要。

---

## 0. 先回答你最关心的三个问题

| 问题 | 一句话答案 |
| --- | --- |
| 只自己用可以吗？ | **可以。** 不发布 = 不触发备案、不触发类目审核、不触发微信审核。个人主体做体验版，成员只加你自己，完全够用。 |
| 布局是不是都要变？ | **前端要重写，但不是"布局全变"。** 信息架构、指标口径、图表配置、配色卡片风格都能平移；真正要重新设计的是 16 张表格在手机上的信息密度。 |
| 怎么让小程序被搜到？ | 对你的内容类型，这条路**基本是关的**（下面第 3 节给官方依据）。自用的话不需要搜到。 |

---

## 1. 「只自己用」的形态对比

自用最反直觉的一点：**小程序不能请求 `http://192.168.x.x` 或 `http://127.0.0.1:8734`**。
`wx.request` 只允许 **HTTPS + 已在小程序后台配置的 request 合法域名**（且域名要备案）。所以自用的难处不在布局，在"数据怎么进来"。

| 方案 | 要做什么 | 成本 | 稳定性 | 备注 |
| --- | --- | --- | --- | --- |
| **0. 不做小程序**：现有页面加「添加到主屏幕」 | 手机浏览器打开 → 添加到主屏幕；建议顺手做一轮手机端适配 | **0.5–1 天** | 高 | **推荐先做这个**，日常自用体验已经接近 App |
| 1. 体验版 + 备案域名 | 备案一个中性域名（个人备案也可以用，但名字别含"金融/证券/投资/基金/理财"）+ HTTPS 反代到本机 + 后台配合法域名 | 域名 1 天 + 备案 7–20 天 | 高 | 最正规；一次投入长期可用 |
| 2. 体验版 + 手机开「调试」跳过域名校验 | 只写小程序，不买域名 | **0 成本** | 低 | 社区通行做法（真机打开调试模式后不校验合法域名），**官方无文档，需实测**；每次可能要重新开调试 |
| 3. 体验版 + 微信云开发 | 把 Python 抓到的数据推到云开发数据库；小程序直连云开发，**不需要域名、不需要备案** | 2–3 天（要改抓数脚本） | 高 | 个人主体可用；注意云开发当前计费（免费额度/基础版套餐需你开通时确认） |

**自用推荐顺序：0 → 3 → 1**（方案 2 只适合"先跑通看看"）。

### 自用形态的操作要点
- 个人主体小程序**项目成员/体验成员各 15 人**上限（[接入指南](https://developers.weixin.qq.com/miniprogram/introduction/)）——只加你自己，够用。
- 开发工具的「预览」码有时效；**长期自用要「上传」→ 设成体验版**，然后把自己加为体验成员，从微信「最近使用的小程序」直接进。
- 未发布小程序**不需要备案**（备案错误码 86369 只在提交发布/上架时校验）；微信开发者工具内开发、真机预览都不涉及备案。历史上对未备案小程序做过清退，体验版属于灰色地带 → **不要把它当长期方案**。

---

## 2. 一旦要给第二个人用，就撞上合规门槛

前提变了，答案就变了。「给朋友用」= 向他人提供证券信息服务，三条硬门槛（均为官方原文）：

1. **个人主体没有"金融业"类目可选。** 个人主体开放类目只有 物流/教育/交通/生活服务/餐饮/旅游/**工具**/商业服务/体育（[类目表](https://developers.weixin.qq.com/miniprogram/product/material/)）。
2. **有营业执照也过不去。** 你的内容对应「金融业-股票信息服务平台」，需**同时**提供 ①《增值电信业务经营许可证》②深交所《专有信息经营许可合同》③上证所《证券信息经营许可合同》（[类目资质](https://developers.weixin.qq.com/miniprogram/product/material/jinrong/gupiaoxinxifuwupingtai.html)）。
3. **备案名字也拦。** 个人主体备案的名称/内容禁含 投资/金融/理财/证券/基金/交易所 等前置审批关键词（[备案FAQ](https://developers.weixin.qq.com/miniprogram/product/record/record_faq.html)）。

另外两条堵退路的：
- `web-view` 组件：**"个人类型的小程序暂不支持使用"**（[web-view](https://developers.weixin.qq.com/miniprogram/dev/component/web-view.html)）→ 套壳 H5 也不行。
- 微信认证**不支持个人主体**（[认证指引](https://developers.weixin.qq.com/miniprogram/product/renzheng)）→ 拿不到名称保护。
- 运营规范 5.7.1 / 5.7.4：超出个人主体类目范围、未申请类目就提供金融内容 → "封禁能力直至下架处理"（[运营规范](https://developers.weixin.qq.com/miniprogram/product/)）。
- 微信按**实际内容**判类目，不是按你填什么类目。

**结论：自用没问题；一旦公开或被举报，风险从"审核不过"升级到"封号"。** 想给别人用，就走方案 0（H5 链接），别走小程序。

---

## 3. 「怎么被搜到」的真相

要分两种"搜到"，难度和门槛完全不同：

| 想被搜到什么 | 机制与前提 | 你的可行性 |
| --- | --- | --- |
| 搜到**小程序本身**（按名称） | 名称全局唯一（[错误码 53013](https://developers.weixin.qq.com/doc/oplatform/openApi/miniprogram-management/basic-info-management/api_checknickname.html)）、有保护期；认证才有名称保护 | 前提是能发布上线 → 第 2 节门槛过不去 |
| 搜到**小程序里的页面内容** | 入口「推广与搜索-微信搜一搜-运营中心-内容接入」，限 **非个人主体 + 6 个一级类目 + 月均 DAU≥5000 + 上架>30 天 + 已备案**，**且不支持股票信息服务类目**（[内容接入](https://developers.weixin.qq.com/miniprogram/introduction/widget/we-search/WXAPAGES.html)） | 对本站内容类型**直接关门** |

技术侧能做的只有：`sitemap.json` + 后台"允许微信索引"（爬虫 UA `mpcrawler`）、名称/简介与实际功能一致、页面 URL 可直达；**web-view 里的内容一律不收录**（[SEO 指南](https://developers.weixin.qq.com/miniprogram/dev/framework/search/seo.html)）。
后台"搜索关键词"最多几个：官方无文档，"10 个"只是社区经验，**不要当依据**。

> 一句话：能不能被搜到，天花板由**类目**决定，不是 SEO 技巧。

---

## 4. 技术方案（真要做小程序前端）

### 4.1 后端：可以原样复用
`app.py` 的 12 个 JSON 接口、`fetch_daily.py`/`scan_volume.py`/`scan_coil.py` 抓数与指标口径**全部不动**：

```
/api/dates  /api/day/<date>  /api/bonds  /api/bond/<code>
/api/volume  /api/volume/dates  /api/volume/<date>  /api/volume/kline/<code>
/api/coil  /api/coil/<date>  /api/bond_detail/<code>  /api/kline_history
/api/refresh  /api/refresh/status
```

改动点只有一个：**出口**。按第 1 节选定的方案，要么加 HTTPS 反代 + 合法域名，要么把 JSON 推到云开发。

### 4.2 前端：`index.html`（2099 行）逐项对照

| 现在 | 小程序里 | 工作量 |
| --- | --- | --- |
| 顶部 8 个横向 tab | `tabBar` 最多 5 个 + 其余做二级页面（建议：今日榜 / 放量 / 蓄势 / 我的） | 中 |
| 16 个 `<table>` | 手机没有 table → 卡片行 或 横滑 `scroll-view` 表格，**信息密度要重新设计（"布局全变"的感觉来自这里）** | **大** |
| 4 处 ECharts + 行内 sparkline | `echarts-for-weixin` 的 `ec-canvas`，或换 uCharts / F2；小屏要减配 | 中 |
| `alert` / `confirm`（5+1 处） | `wx.showModal` | 小 |
| `prompt`（1 处） | 无原生对应，自研弹窗 | 小 |
| `navigator.clipboard`（3 处） | `wx.setClipboardData` | 小 |
| `localStorage`（模拟仓） | `wx.setStorageSync` | 小 |
| `document.getElementById`（91 处） | 数据绑定 `setData`，整段逻辑要改写 | **大** |
| `window.open`（新闻外链） | **打不开外部网页**，只能复制链接 | 小但功能损失 |
| 键盘 ← / → 翻券 | 手势或按钮 | 小 |
| px 布局 | rpx + `scroll-view`；WXSS 无 `*` 通配、无 body/html 选择器；flex 基本可用 | 中 |
| 无 | 新增 `onShareAppMessage`、下拉刷新（自用可省） | 小 |

**平移得了的**：信息架构、指标口径（热度/放量/蓄势/量价验证）、图表配置、配色与卡片风格。
**必须重做的**：表格信息密度、图表尺寸、全部 DOM 与浏览器 API、导航结构。
**会消失的**：榜单变迁页的新闻外链、键盘翻券——这两个是你现在页面里挺讨喜的功能，做之前先想清楚舍不舍得。

### 4.3 自用版建议砍到最小
只做 3 个页面就够日常用，工作量从"两周"降到"两三天"：
1. **今日榜**（热度 Top30 + 放量 + 蓄势三张卡片列表，点开进详情）
2. **个券详情**（分时 + K 线 + 均线，`ec-canvas` 一张图）
3. **我的**（自选列表，`wx.setStorageSync` 存本地）

量价验证、模拟仓、指数对比、榜单变迁先不做——那些是"分析"场景，手机上也不舒服，回电脑看就行。

---

## 5. 风险与待确认项

| 项 | 说明 |
| --- | --- |
| 「真机开调试跳过域名校验」 | 社区通行做法，**官方无文档**；用之前先实测一版（真机打开调试 → 重新进小程序 → 看 request 是否放行） |
| 云开发计费 | 免费额度/基础版套餐政策变动过，**开通时以控制台报价为准** |
| 未备案小程序体验版 | 官方口径是"未备案不能上架"，体验版不受影响；但历史上有过清退动作，**别当长期方案** |
| 数据源 | 现有数据抓自东方财富/腾讯行情。**自用属于个人研究，风险低；一旦公开分发就涉及交易所行情信息授权问题** |
| 域名备案名称 | 若走方案 1，个人网站备案名不要用"可转债/证券/投资"等字样，用中性的（如"XX工具箱"） |
| `index.html` 与小程序双份前端 | 两套前端会长期分叉。若要长期维护，考虑 uni-app / Taro 一套代码出 H5 + 小程序（自用场景**不值得**，直接双份更省事） |

---

## 6. 建议的下一步（按优先级）

1. **今天**：手机上把现有页面"添加到主屏幕"，用两天，确认"自用"到底缺什么（是缺原生手感，还是单纯觉得链接麻烦）。
2. **如果真要小程序**：先只做方案 2（0 成本跑通取数），验证"手机开调试能不能跳过域名校验"这一个假设。
3. 假设成立 → 按 4.3 做 3 个页面，1–3 天出货。
4. 假设不成立 → 走方案 3（云开发）或方案 1（备案域名）。

**关键判断**：自用场景下，方案 0（不做小程序）的投入产出比远高于小程序。真要做，先验证取数通道，再动布局。
